import { onMounted, onUnmounted } from "vue"
import Choices from 'choices.js';

export function useChoiceJs(id) {
    onMounted(() => {
        // const selectElement = document.querySelector('#'+id);
        // const choices = new Choices(selectElement);
    })

}
